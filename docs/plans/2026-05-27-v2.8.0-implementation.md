# v2.8.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Реалізувати релі́з v2.8.0 плагіна `android-review` — додати два правила (`meta/dev-bundle-match`, `webview/cleartext-traffic-disabled`), розширити whitelist категорій агента (фіксує орфан-регресію `perf/`), та передати в агент імʼя батьківської папки проєкту з orchestrator-промпта.

**Architecture:** Плагін складається з markdown-файлів правил, prompt-файла одного агента й двох slash-команд. Усі зміни — текстові редагування markdown/JSON, без компіляції чи юніт-тестів. Верифікація — ручний прогін `/android-review:android-review` на test-проєктах + статичні перевірки структури правил (`grep` секцій frontmatter і body).

**Tech Stack:** Markdown (правила, агент, команди), JSON (plugin manifest), Bash (orchestrator). Plugin runtime — Claude Code 2.1.x.

**Constraint:** Існуючий патерн релізів — **один коміт на реліз** (див. `git log --oneline`: `feat: v2.7.0 — …`, `feat: v2.6.0 — …`). Тому проміжні таски стейджать зміни, але **не** комітять. Фінальний task робить один release-commit.

**Spec:** `docs/specs/2026-05-27-meta-and-cleartext-rules-design.md`

---

## File Structure

**Create:**
- `rules/meta/dev-bundle-match.md` — нове правило #1 (severity: suspicious)
- `rules/webview/cleartext-traffic-disabled.md` — нове правило #2 (severity: critical)
- `rules/meta/` — нова директорія (створюється фактом запису першого файлу)

**Modify:**
- `agents/functional-validator.md` — рядки 3, 12, 35: розширити whitelist категорій
- `commands/android-review.md` — Step 4: передати в агент `Project parent folder`
- `rules/_schema.md` — синхронізувати список категорій
- `.claude-plugin/plugin.json` — bump `version` 2.7.0 → 2.8.0
- `CHANGELOG.md` — додати запис v2.8.0

**No changes to:**
- `commands/android-review-init.md` — нові правила читають уже наявні джерела (gradle, manifest), `init`-шаблон не зачіпається.
- `.claude/CLAUDE.md`-template у проєктах — backward compatible.

---

## Task 1 — Write Rule #2 (`webview/cleartext-traffic-disabled`)

> Спочатку пишемо простіше правило #2 (чиста manifest-перевірка, без orchestrator-залежностей), потім #1.

**Files:**
- Create: `rules/webview/cleartext-traffic-disabled.md`

- [ ] **Step 1.1: Create the rule file**

Write the following exact content to `rules/webview/cleartext-traffic-disabled.md`:

````markdown
---
id: webview/cleartext-traffic-disabled
severity: critical
category: webview
applies-to:
  - app/src/main/AndroidManifest.xml
  - app/src/main/res/xml/network_security_config.xml
since: "2.8.0"
---

# Cleartext-трафік у Manifest заборонений (для WebView / CustomTabs)

## Інваріант

Атрибут `android:usesCleartextTraffic` на елементі `<application>` у
`app/src/main/AndroidManifest.xml` **не може мати значення `"true"`**.
Допустимі стани:
- атрибут **відсутній** і `targetSdkVersion >= 28` (дефолт OS = false);
- атрибут явно `"false"`.

Інваріант існує тому, що апки використовують **WebView і/або CustomTabs**
як основну поверхню контенту — `true` означає, що будь-який HTTP
(не-HTTPS) URL, відкритий у WebView/CustomTabs/HTTP-клієнті апки,
передаватиметься у відкритому вигляді.

NSC (`networkSecurityConfig`) з `cleartextTrafficPermitted="true"` може
обходити manifest-заборону — це емітимо як `OBSERVATION`, рішення
легітимності залишаємо за людиною.

## Як перевірити

Це **manifest-перевірка з опціональним walk-у NSC**, а не grep по проєкту.

1. Прочитати `app/src/main/AndroidManifest.xml`.
2. Знайти елемент `<application ...>` (типово один).
3. Витягти `targetSdkVersion`: з `defaultConfig.targetSdk` у
   `app/build.gradle(.kts)` (це source of truth — gradle overrides
   manifest). Якщо в gradle не задано — взяти з `<uses-sdk
   android:targetSdkVersion="..."/>` у манІфесті. Якщо ніде не задано —
   вважати `targetSdk = 1` (worst case).
4. Перевірити `android:usesCleartextTraffic`:
   - `"true"` → finding `CRITICAL`.
   - `"false"` → інваріант виконано (Перевірені інваріанти).
   - **відсутній** + `targetSdk >= 28` → інваріант виконано.
   - **відсутній** + `targetSdk < 28` → finding `CRITICAL` (історичний
     дефолт OS = true).
5. Незалежно від п.4, якщо `<application>` має
   `android:networkSecurityConfig="@xml/<name>"`:
   - відкрити `app/src/main/res/xml/<name>.xml`;
   - якщо референс битий (файл відсутній) → emit `OBSERVATION`
     («NSC файл `@xml/<name>` не знайдено»);
   - `<base-config cleartextTrafficPermitted="true">` → emit
     `OBSERVATION` («NSC дозволяє cleartext глобально — обходить
     manifest-заборону»);
   - `<domain-config cleartextTrafficPermitted="true">` → emit
     `OBSERVATION` («NSC дозволяє cleartext для конкретних доменів —
     перевірте навмисність»).
6. Перевіряється лише `app/src/main/AndroidManifest.xml`.
   `debug/AndroidManifest.xml` ігнорується (легітимний debug-overlay).

## Як виглядає поломка

```xml
<!-- app/src/main/AndroidManifest.xml -->
<application
    android:usesCleartextTraffic="true"
    android:label="@string/app_name"
    ...>
    <!-- WebView/CustomTabs усередині пропускатимуть http:// у відкритому вигляді -->
</application>
```

Або через NSC (емітимо як OBSERVATION):

```xml
<!-- app/src/main/res/xml/network_security_config.xml -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
```

## Як виглядає правильно

Варіант А — явно `false`:

```xml
<application
    android:usesCleartextTraffic="false"
    ...>
```

Варіант Б — атрибут відсутній (рекомендовано, бо дефолт на API 28+ і так `false`):

```xml
<application
    android:label="@string/app_name"
    ...>
```

## Як доповідати

Manifest-варіант:

```
[webview/cleartext-traffic-disabled] CRITICAL
  app/src/main/AndroidManifest.xml:<line>
  <application android:usesCleartextTraffic="true"> — апка явно дозволяє HTTP-трафік. WebView/CustomTabs/HTTP-клієнти можуть відкривати http:// URL у відкритому вигляді, що є витоком даних.
  Як виправити: видаліть атрибут (на API 28+ дефолт = false) або явно поставте android:usesCleartextTraffic="false".
  Див.: rules/webview/cleartext-traffic-disabled.md
```

NSC-варіант:

```
[webview/cleartext-traffic-disabled] OBSERVATION
  app/src/main/res/xml/<nsc-file>.xml:<line>
  NSC-конфіг дозволяє cleartextTrafficPermitted="true" (<base-config|domain-config>). Це обходить manifest-заборону для відповідних доменів.
  Як виправити: переконайтеся, що дозвіл навмисний (legacy backend без TLS, локальний сервер); інакше — заберіть атрибут із NSC.
  Див.: rules/webview/cleartext-traffic-disabled.md
```

## Виключення

Допускається через `accepted-deviations` у `.claude/CLAUDE.md` з reason.
Легітимні причини: інтеграція з legacy-бекендом, який ще не на TLS;
локальний dev-сервер, доступний тільки в debug-зборці. Рекомендоване
обхідне рішення — винести cleartext-дозвіл у `debug/AndroidManifest.xml`-overlay,
тоді release-маніфест чистий і exception не потрібен.
````

- [ ] **Step 1.2: Verify file structure**

Run:

```bash
grep -cE '^## (Інваріант|Як перевірити|Як виглядає поломка|Як виглядає правильно|Як доповідати|Виключення)$' rules/webview/cleartext-traffic-disabled.md
```

Expected output: `6`

Run:

```bash
head -10 rules/webview/cleartext-traffic-disabled.md
```

Expected output (frontmatter):

```
---
id: webview/cleartext-traffic-disabled
severity: critical
category: webview
applies-to:
  - app/src/main/AndroidManifest.xml
  - app/src/main/res/xml/network_security_config.xml
since: "2.8.0"
---
```

- [ ] **Step 1.3: Stage**

```bash
git add rules/webview/cleartext-traffic-disabled.md
```

---

## Task 2 — Write Rule #1 (`meta/dev-bundle-match`)

**Files:**
- Create: `rules/meta/dev-bundle-match.md` (also creates `rules/meta/` directory)

- [ ] **Step 2.1: Create the rule file**

Write the following exact content to `rules/meta/dev-bundle-match.md`:

````markdown
---
id: meta/dev-bundle-match
severity: suspicious
category: meta
applies-to:
  - app/build.gradle.kts
  - app/build.gradle
since: "2.8.0"
---

# Очікуваний test bundle для розробника на час code review

## Інваріант

Коли проєкт належить одному з відомих розробників (визначається імʼям
**батьківської папки** проєкту), значення `defaultConfig.applicationId`
у `app/build.gradle(.kts)` має точно дорівнювати закріпленому test-бандлу
цього розробника (див. каталог нижче). На час code review та тестування
дев перемикає `applicationId` на свій test-бандл; production-значення
повертається лише перед релізом. Розбіжність сигналізує: або дев забув
перемкнути бандл, або проєкт лежить у не-своєму каталозі.

## Каталог dev → test bundle

| `<DevName>` (батьківська папка) | `applicationId` |
|---|---|
| `Dima`    | `com.proxima.dfm` |
| `Elchin`  | `easy.sudoku.puzzle.solver.free` |
| `Grisha`  | `com.ecffri.arrows` |
| `Nikita`  | `com.mobile.legends` |
| `Marina`  | `com.mergegames.gossipharbor` |
| `Danik`   | `com.haoplay.game.and.exilium` |

Імена case-sensitive (саме `Dima`, не `dima`).

## Як перевірити

Це **резолв identity з orchestrator-промпта + читання gradle**, а не grep-перевірка.

1. У dispatch-промпті до агента orchestrator має передавати рядок
   `Project parent folder: <name>` (див. `commands/android-review.md` Step 4).
   Прочитати це значення. Якщо рядка немає — правило не може застосуватися
   (помилка orchestrator'а) → emit `OBSERVATION` («orchestrator не передав
   Project parent folder — пропустіть або оновіть commands/android-review.md»).
2. Якщо `<name>` точно збігається з одним із ключів каталогу
   (case-sensitive) — `expected = mapping[<name>]`. Перейти до п.3.
3. Якщо `<name>` НЕ з каталогу — правило **не застосовне** → виводиться
   під «Пропущені перевірки» з причиною
   `project parent folder "<name>" not in known devs catalog`,
   жодного фіндінга.
4. Прочитати `app/build.gradle.kts` (або `app/build.gradle`), знайти блок
   `android { defaultConfig { applicationId = "..." } }`. Витягти точне
   значення літерала.
5. Порівняти exact string з `expected`:
   - збіг → інваріант виконано, додати у «Перевірені інваріанти»;
   - розбіжність або `applicationId` відсутній → finding `SUSPICIOUS`.
6. Варіанти/flavor-суфікси (`debug { applicationIdSuffix = ".debug" }`,
   `productFlavors { … applicationId = … }`) у v1 **не враховуються** —
   перевіряємо тільки `defaultConfig.applicationId`. Якщо в майбутньому
   зʼявиться кейс, де release-flavor легітимно перевизначає → внести через
   `accepted-deviations` у проєктному CLAUDE.md.

## Як виглядає поломка

Проєкт у `~/StudioProjects/Dima/some-app/` (тобто orchestrator передав
`Project parent folder: Dima`), файл `app/build.gradle.kts`:

```kotlin
android {
    defaultConfig {
        applicationId = "com.realproduct.shipped"   // ❌ не test-bundle Dima
        // ...
    }
}
```

## Як виглядає правильно

Той самий шлях:

```kotlin
android {
    defaultConfig {
        applicationId = "com.proxima.dfm"   // ✅ test-bundle Dima
        // ...
    }
}
```

## Як доповідати

```
[meta/dev-bundle-match] SUSPICIOUS
  app/build.gradle.kts:<line of applicationId>
  Проєкт у каталозі `<DevName>`, але defaultConfig.applicationId = "<actual>" — очікувався test-bundle "<expected>". Це може значити, що дев забув перемкнути бандл на test перед рев'ю, або проєкт у не-своєму каталозі.
  Як виправити: змініть `defaultConfig.applicationId` на "<expected>" перед рев'ю/тестуванням; перед релізом — поверніть production-значення.
  Див.: rules/meta/dev-bundle-match.md (каталог dev→bundle).
```

## Виключення

Допускається через `accepted-deviations` у `.claude/CLAUDE.md` з явною причиною:
- maintenance-рев'ю вже випущеної апки, де bundle = production;
- дев тимчасово допомагає в проєкті іншого дева;
- проєкт легітимно живе поза `~/StudioProjects/` (зовнішнє місце) — там
  батьківська папка не з каталогу і так пропускає правило тихо, exception
  не потрібен.
````

- [ ] **Step 2.2: Verify file structure**

Run:

```bash
grep -cE '^## (Інваріант|Як перевірити|Як виглядає поломка|Як виглядає правильно|Як доповідати|Виключення)$' rules/meta/dev-bundle-match.md
```

Expected output: `6`

Run:

```bash
head -10 rules/meta/dev-bundle-match.md
```

Expected output (frontmatter):

```
---
id: meta/dev-bundle-match
severity: suspicious
category: meta
applies-to:
  - app/build.gradle.kts
  - app/build.gradle
since: "2.8.0"
---
```

Verify the catalog table is present:

```bash
grep -cE '^\| `(Dima|Elchin|Grisha|Nikita|Marina|Danik)` ' rules/meta/dev-bundle-match.md
```

Expected output: `6`

- [ ] **Step 2.3: Stage**

```bash
git add rules/meta/dev-bundle-match.md
```

---

## Task 3 — Update orchestrator dispatch (`commands/android-review.md`)

> Це **передумова** для Rule #1. Без цього кроку Rule #1 завжди буде в «Пропущені» через відсутність `Project parent folder` у промпті.

**Files:**
- Modify: `commands/android-review.md:78-87` (Step 4 dispatch block)

- [ ] **Step 3.1: Locate the dispatch block**

Confirm current content of Step 4 in `commands/android-review.md`:

```bash
sed -n '78,87p' commands/android-review.md
```

Expected (current):

```
## Step 4 — Dispatch the functional-validator

Use the `Task` tool with `subagent_type: functional-validator` and this
prompt body. Substitute the discovered `PLUGIN_ROOT`:

```
Plugin root: <PLUGIN_ROOT>

Run a full functional Android review on the project at the current working directory. Follow your system prompt's procedure exactly. Return the markdown report only.
```
```

- [ ] **Step 3.2: Edit Step 4 to compute and pass parent folder**

Use the Edit tool to replace this block:

**Old string:**

````
## Step 4 — Dispatch the functional-validator

Use the `Task` tool with `subagent_type: functional-validator` and this
prompt body. Substitute the discovered `PLUGIN_ROOT`:

```
Plugin root: <PLUGIN_ROOT>

Run a full functional Android review on the project at the current working directory. Follow your system prompt's procedure exactly. Return the markdown report only.
```
````

**New string:**

````
## Step 4 — Dispatch the functional-validator

Compute project's parent folder name (used by `meta/*` rules). Use Bash:

```
basename "$(dirname "$PWD")"
```

Bind that exact stdout (no trim/whitespace transforms beyond the natural
trailing newline strip) as `PROJECT_PARENT_FOLDER`. If the command fails
or returns empty — bind `PROJECT_PARENT_FOLDER` to literal `<unknown>`.

Use the `Task` tool with `subagent_type: functional-validator` and this
prompt body. Substitute the discovered `PLUGIN_ROOT` and `PROJECT_PARENT_FOLDER`:

```
Plugin root: <PLUGIN_ROOT>
Project parent folder: <PROJECT_PARENT_FOLDER>

Run a full functional Android review on the project at the current working directory. Follow your system prompt's procedure exactly. Return the markdown report only.
```
````

- [ ] **Step 3.3: Verify the edit**

Run:

```bash
grep -nE 'Project parent folder|PROJECT_PARENT_FOLDER|basename "\$\(dirname' commands/android-review.md
```

Expected output (4 hits, lines may differ slightly):

```
NN:basename "$(dirname "$PWD")"
NN:Bind that exact stdout ... as `PROJECT_PARENT_FOLDER`. ...
NN:prompt body. Substitute the discovered `PLUGIN_ROOT` and `PROJECT_PARENT_FOLDER`:
NN:Project parent folder: <PROJECT_PARENT_FOLDER>
```

- [ ] **Step 3.4: Stage**

```bash
git add commands/android-review.md
```

---

## Task 4 — Update agent discovery whitelist (`agents/functional-validator.md`)

> Це активаційний крок: без нього новий `meta/`-каталог не дискаверяться, і **попутно** лагодиться орфан-регресія `perf/`.

**Files:**
- Modify: `agents/functional-validator.md:3` (description)
- Modify: `agents/functional-validator.md:12` (Your job paragraph)
- Modify: `agents/functional-validator.md:35` (Step 1 — Discover rules)

- [ ] **Step 4.1: Verify current state (3 occurrences of old whitelist)**

```bash
grep -n 'rules/{flow,webview,crypto}' agents/functional-validator.md
```

Expected: 3 lines (3, 12, 35).

- [ ] **Step 4.2: Replace all three occurrences**

Use the Edit tool with `replace_all: true`:

**Old string:**

```
rules/{flow,webview,crypto}/
```

**New string:**

```
rules/{flow,webview,crypto,perf,meta}/
```

- [ ] **Step 4.3: Verify the edit**

```bash
grep -n 'rules/{flow,webview,crypto,perf,meta}' agents/functional-validator.md
```

Expected: 3 lines (3, 12, 35).

```bash
grep -n 'rules/{flow,webview,crypto}/' agents/functional-validator.md
```

Expected: NO matches (empty stdout).

- [ ] **Step 4.4: Stage**

```bash
git add agents/functional-validator.md
```

---

## Task 5 — Update `rules/_schema.md`

> Косметика: схема має не брехати про дозволені категорії.

**Files:**
- Modify: `rules/_schema.md:13` (frontmatter `category` enum)
- Modify: `rules/_schema.md:64-78` (Categories section)

- [ ] **Step 5.1: Update the frontmatter category enum**

Use the Edit tool:

**Old string:**

```
category: flow | webview | crypto      # duplicates first id segment
```

**New string:**

```
category: flow | webview | crypto | perf | meta   # duplicates first id segment
```

- [ ] **Step 5.2: Update the Categories section**

Use the Edit tool:

**Old string:**

```
## Categories

- **`flow/`** — application-startup and runtime behavior (UUID,
  push init, attribution, routing, redirect method).
- **`webview/`** — WebView/CustomTabs configuration and host
  Activity requirements.
- **`crypto/`** — POST-data encoding pattern (file paths NOT
  pinned; only the pattern).
- **`perf/`** — performance and pitfall observations
  (`severity: observation` only). Surfaces actionable
  improvements: startup-blocking patterns, WebView UX/perf
  pitfalls, runtime-decrypt cost. NEVER blocks the verdict —
  these are advisory, not contracts.

Style/security/obfuscation categories from v1.x are deleted —
they don't map to functional flows.
```

**New string:**

```
## Categories

- **`flow/`** — application-startup and runtime behavior (UUID,
  push init, attribution, routing, redirect method).
- **`webview/`** — WebView/CustomTabs configuration and host
  Activity requirements; includes baseline Manifest-level security
  invariants tied to WebView traffic (e.g., cleartext).
- **`crypto/`** — POST-data encoding pattern (file paths NOT
  pinned; only the pattern).
- **`perf/`** — performance and pitfall observations
  (`severity: observation` only). Surfaces actionable
  improvements: startup-blocking patterns, WebView UX/perf
  pitfalls, runtime-decrypt cost. NEVER blocks the verdict —
  these are advisory, not contracts.
- **`meta/`** — project-level identity invariants (owner ↔ bundle,
  applicationId hygiene на час code review). Не звʼязано з runtime
  поведінкою — стосується «чий і який» проєкту.

Style/security/obfuscation categories from v1.x are deleted —
they don't map to functional flows.
```

- [ ] **Step 5.3: Verify**

```bash
grep -nE 'category: flow \| webview \| crypto \| perf \| meta|^- \*\*`meta/`\*\*' rules/_schema.md
```

Expected: 2 lines (frontmatter line and categories bullet).

- [ ] **Step 5.4: Stage**

```bash
git add rules/_schema.md
```

---

## Task 6 — Bump version in `.claude-plugin/plugin.json`

**Files:**
- Modify: `.claude-plugin/plugin.json:3`

- [ ] **Step 6.1: Edit the version field**

Use the Edit tool:

**Old string:**

```
  "version": "2.7.0",
```

**New string:**

```
  "version": "2.8.0",
```

- [ ] **Step 6.2: Verify the JSON is still valid and version is bumped**

```bash
python3 -c 'import json,sys; v=json.load(open(".claude-plugin/plugin.json"))["version"]; sys.exit(0 if v=="2.8.0" else 1); print(v)'
echo "exit=$?"
```

Expected: `exit=0`

- [ ] **Step 6.3: Stage**

```bash
git add .claude-plugin/plugin.json
```

---

## Task 7 — Add v2.8.0 entry to `CHANGELOG.md`

**Files:**
- Modify: `CHANGELOG.md` (prepend new section above the existing v2.7.0 entry)

- [ ] **Step 7.1: Locate the v2.7.0 section**

```bash
grep -n '^## \[' CHANGELOG.md | head -3
```

This will show the top entries. Note the exact heading text for v2.7.0 — it will be used as the anchor for prepending.

- [ ] **Step 7.2: Prepend v2.8.0 entry**

Use the Edit tool. **Old string** is the first `## [` heading found in Step 7.1 (assume `## [2.7.0] — 2026-05-XX` style; if the actual heading differs, use the exact line):

**Old string:** (replace with the actual first `## [` heading you observed)

```
## [2.7.0]
```

**New string:**

```
## [2.8.0] — 2026-05-27

### Added
- **`meta/dev-bundle-match`** (`suspicious`) — нова категорія `meta/`.
  Перевіряє, що `defaultConfig.applicationId` у проєкті відомого
  розробника (визначається імʼям батьківської папки) збігається з
  його закріпленим test-бандлом. 6 девів у каталозі: Dima, Elchin,
  Grisha, Nikita, Marina, Danik. Тиха пропустка, якщо парент не з
  каталогу.
- **`webview/cleartext-traffic-disabled`** (`critical`) — забороняє
  `android:usesCleartextTraffic="true"` у `app/src/main/AndroidManifest.xml`.
  Враховує `targetSdk < 28` (без атрибута = критично через історичний
  дефолт OS). NSC з `cleartextTrafficPermitted="true"` емітимо як
  `OBSERVATION`.

### Changed
- **`agents/functional-validator.md`** — whitelist категорій
  `rules/{flow,webview,crypto}` → `rules/{flow,webview,crypto,perf,meta}`
  у трьох місцях (description, Your job, Step 1). **Попутно лагодить
  регресію з v2.1.0**: правила з `rules/perf/` тепер фактично
  дискаверяться й виконуються (раніше були орфанами; severity =
  observation, вердикт не може погіршитися).
- **`commands/android-review.md`** — Step 4 обчислює
  `PROJECT_PARENT_FOLDER` через `basename "$(dirname "$PWD")"` і
  передає в dispatch-промпт як рядок `Project parent folder: <name>`.
  Без цього `meta/`-правила не мають змоги резолвити `<DevName>`
  (агент не має Bash).
- **`rules/_schema.md`** — список категорій оновлено
  (додано `meta`, `perf` залишається).

## [2.7.0]
```

- [ ] **Step 7.3: Verify**

```bash
grep -n '^## \[2\.' CHANGELOG.md | head -3
```

Expected: перший хіт — `## [2.8.0] — 2026-05-27`, другий — `## [2.7.0]`.

- [ ] **Step 7.4: Stage**

```bash
git add CHANGELOG.md
```

---

## Task 8 — End-to-end manual verification

> Виконується **до** фінального коміту. Якщо щось завалюється — відкатати або поправити інкрементально.

**Test fixtures:** два мінімальні Android-проєкти (можна реальні з `~/StudioProjects/`):
- `~/StudioProjects/Dima/<any>/` — для перевірки meta-правила happy/sad path.
- Тимчасовий fake-проєкт у `/tmp/foo-app/` — для перевірки skip-path.

- [ ] **Step 8.1: Verify `meta/dev-bundle-match` HAPPY (matching bundle)**

В одному з реальних проєктів Dima:

```bash
cd ~/StudioProjects/Dima/<some-app>
# переконатися, що defaultConfig.applicationId = "com.proxima.dfm"
grep -A 1 'defaultConfig' app/build.gradle.kts | grep applicationId
```

Очікувано: значення = `"com.proxima.dfm"`.

Запустити рев'ю:

```
claude
/android-review:android-review
```

Очікувано в звіті:
- розділ «Перевірені інваріанти» містить `meta/dev-bundle-match`;
- розділ «🔴 Критичні баги функціональної логіки» не містить цього правила;
- розділ «⚠️ Підозрілі патерни» не містить цього правила.

- [ ] **Step 8.2: Verify `meta/dev-bundle-match` SAD (wrong bundle)**

Тимчасово замінити `applicationId` на чужий:

```bash
# у тому ж проєкті Dima
# (за можливості — створити тимчасову гілку щоб не псувати master)
# applicationId = "com.proxima.dfm" → "com.someone-else.app"
```

Запустити `/android-review:android-review` ще раз.

Очікувано:
- у розділі «⚠️ Підозрілі патерни» з'являється finding:
  ```
  [meta/dev-bundle-match] SUSPICIOUS
  app/build.gradle.kts:<line>
  Проєкт у каталозі `Dima`, але defaultConfig.applicationId = "com.someone-else.app" — очікувався test-bundle "com.proxima.dfm". ...
  ```

Повернути `applicationId` назад.

- [ ] **Step 8.3: Verify `meta/dev-bundle-match` SKIP (unknown parent)**

```bash
mkdir -p /tmp/foo-app/app
cat > /tmp/foo-app/app/build.gradle.kts <<'EOF'
android {
    defaultConfig {
        applicationId = "com.anything.here"
    }
}
EOF
cd /tmp/foo-app
claude
/android-review:android-review
```

Очікувано в звіті:
- розділ «Пропущені перевірки» містить `meta/dev-bundle-match` з причиною
  `project parent folder "tmp" not in known devs catalog`;
- жодного `[meta/dev-bundle-match]` фіндінга в інших розділах.

Cleanup:

```bash
rm -rf /tmp/foo-app
```

- [ ] **Step 8.4: Verify `webview/cleartext-traffic-disabled` HAPPY (default)**

На будь-якому з реальних проєктів з `targetSdk >= 28`:

```bash
cd ~/StudioProjects/<DevName>/<some-app>
grep -E 'usesCleartextTraffic|targetSdk' app/src/main/AndroidManifest.xml app/build.gradle.kts 2>/dev/null
```

Якщо `usesCleartextTraffic` відсутній і `targetSdk >= 28`:

```
claude
/android-review:android-review
```

Очікувано: `webview/cleartext-traffic-disabled` у «Перевірені інваріанти».

- [ ] **Step 8.5: Verify `webview/cleartext-traffic-disabled` SAD**

Тимчасово додати `android:usesCleartextTraffic="true"` до `<application>` у `app/src/main/AndroidManifest.xml`. Запустити `/android-review:android-review`.

Очікувано:
- розділ «🔴 Критичні баги функціональної логіки» містить
  `[webview/cleartext-traffic-disabled] CRITICAL`;
- вердикт = `🔴 НЕ ГОТОВО`.

Відкатати правку.

- [ ] **Step 8.6: Verify `perf/` regression fix**

На будь-якому проєкті з мінімально нетривіальним `Application.onCreate`:

```
claude
/android-review:android-review
```

Очікувано: розділ «ℹ️ Спостереження» містить **щонайменше один** finding з `perf/` (наприклад `perf/startup-blocking` або `perf/webview-pitfalls` або `perf/runtime-decrypt-cost`). До v2.8.0 таких findings не було взагалі — їх поява підтверджує fix орфана.

> Якщо проєкт чистий і perf-правила нічого не находять — це теж валідний результат, але хоча б на одному з шести проєктів має зʼявитися хоч одне perf-спостереження. Інакше — перевірити, чи Step 4.3 справді змінив всі 3 рядки.

- [ ] **Step 8.7: Verify all rules still discovered**

```bash
# симулювати, що бачить агент
ls rules/{flow,webview,crypto,perf,meta}/*.md | grep -v '^_'
```

Очікувано: список з усіма правилами, серед них `rules/meta/dev-bundle-match.md` і `rules/webview/cleartext-traffic-disabled.md`.

```bash
ls rules/{flow,webview,crypto,perf,meta}/*.md | wc -l
```

Очікувано: `13` (11 існуючих + 2 нових).

---

## Task 9 — Release commit (final)

> Один коміт на реліз — патерн команди (див. `git log --oneline`).

- [ ] **Step 9.1: Review staged changes**

```bash
git status
git diff --cached --stat
```

Очікувано (приблизно):

```
 .claude-plugin/plugin.json                                        | 2 +-
 CHANGELOG.md                                                       | NN ++
 agents/functional-validator.md                                     |  6 +--
 commands/android-review.md                                         | NN ++
 rules/_schema.md                                                   |  9 +-
 rules/meta/dev-bundle-match.md                                     | NN +
 rules/webview/cleartext-traffic-disabled.md                        | NN +
 7 files changed, ...
```

Якщо щось відсутнє — повернутися до відповідного task і дізнатися чому не застейджено.

- [ ] **Step 9.2: Verify nothing unintended is staged**

```bash
git diff --cached --name-only
```

Очікувано — рівно ці 7 шляхів:

```
.claude-plugin/plugin.json
CHANGELOG.md
agents/functional-validator.md
commands/android-review.md
rules/_schema.md
rules/meta/dev-bundle-match.md
rules/webview/cleartext-traffic-disabled.md
```

Якщо у списку також `docs/specs/2026-05-27-meta-and-cleartext-rules-design.md` або
`docs/plans/2026-05-27-v2.8.0-implementation.md` — окремо обговорити з користувачем,
чи комітити дизайн і план у тому ж релізі (зазвичай так — щоб історія була повна).

- [ ] **Step 9.3: Create the release commit**

```bash
git commit -m "$(cat <<'EOF'
feat: v2.8.0 — meta/dev-bundle-match + webview/cleartext-traffic-disabled + perf/ orphan fix

- Add rules/meta/ category and rules/meta/dev-bundle-match.md (suspicious):
  validates defaultConfig.applicationId against the dev's locked test-bundle,
  resolving the dev from the project's parent folder name (Dima, Elchin,
  Grisha, Nikita, Marina, Danik). Silent skip if parent isn't in the catalog.
- Add rules/webview/cleartext-traffic-disabled.md (critical): bans
  android:usesCleartextTraffic="true" in AndroidManifest.xml; accounts for
  targetSdk < 28 (no attribute then = critical due to legacy OS default).
  NSC cleartextTrafficPermitted emits OBSERVATION.
- Expand agent category whitelist in agents/functional-validator.md from
  {flow,webview,crypto} to {flow,webview,crypto,perf,meta}. This also fixes
  a regression from v2.1.0: rules under rules/perf/ were silently orphaned —
  agent never discovered them. They are observation-only, so verdicts can't
  regress.
- Extend commands/android-review.md Step 4 to compute and pass
  "Project parent folder: <name>" in the dispatch prompt, so meta/* rules
  can resolve <DevName> without Bash (agent has no Bash tool).
- Update rules/_schema.md categories list to include meta and align meta
  description with the rule.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 9.4: Verify the commit**

```bash
git log --oneline -1
```

Expected: один рядок, що починається з нового SHA та повідомленням
`feat: v2.8.0 — meta/dev-bundle-match + webview/cleartext-traffic-disabled + perf/ orphan fix`.

```bash
git show --stat HEAD
```

Expected: 7 файлів змінено.

---

## Post-release (out of plan scope)

- **Push** в апстрім — за рішенням користувача (не автоматизуємо).
- **Smoke прогін** `/android-review:android-review` на всіх 6 каталогах
  девів — щоб переконатися, що нові правила не зламали існуючі рев'ю.
- **Документація**: README не потребує оновлення, бо нові правила
  підтягуються автоматично через `/android-review:android-review` без
  додаткової конфігурації.
